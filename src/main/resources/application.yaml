spring:
  profiles:
    active:
      - prod

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: 123456

---

spring:
  profiles: prod

  rabbitmq:
    host: ${CLOUDAMQP_URL}